import { boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

import { SearchInput } from '@ui';

<Meta title="Design System/ui/SearchInput" component={SearchInput} />

# SearchInput

SearchInput is used to inform the user that he is going to search for something through typing.

<Preview withToolbar>
  <Story name="default">
    <SearchInput
      placeholder="Placeholder..."
      disabled={boolean('Disabled', false)}
      onChange={action('search-input-change')}
      allowClear={boolean('Allow Clear', false)}
      onPressEnter={action('search-input-press-enter')}
    />
  </Story>
</Preview>
