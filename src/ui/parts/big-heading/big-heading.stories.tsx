import { storiesOf } from '@storybook/react';

import { BigHeading } from './big-heading.component';

storiesOf('Design System/ui/BigHeading', module).add('default', () => (
  <BigHeading>Big heading</BigHeading>
));
