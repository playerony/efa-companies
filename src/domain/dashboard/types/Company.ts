export interface Company {
  name: string;
  type: string;
  symbol: string;
  region: string;
  currency: string;
  timezone: string;
  marketOpen: string;
  matchScore: string;
  marketClose: string;
}
